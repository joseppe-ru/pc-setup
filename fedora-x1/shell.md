# Ja


## install
```bash
sudo dnf install zsh
sudo dnf install kitty
```
install lazy vim (like on website description)
install oh-my-zsh (like on website)


## Standards
```bash
chsh -s $(which zsh)
```

Systemsettings -> default applications -> Terminal "kitty"
Systemsettings -> Shortcuts -> rm terminal kombination, add kitty and combination




## Config 
copy configs (monst recent on arch-knarf)
clone git-repos for zsh plugins in `~/.oh_my_zsh/plugins/<repo/plugin_name>`



